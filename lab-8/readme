How to Build and Run:

Steps (in Google Colab):
1. Set up runtime environment
    Go to runtime tab -> change runtime type -> set hardware accelerator to T4

2. Add lab8.cu, stb_image_write.h, stb_image.h, and test.bmp to the Colab workspace's files.

3. Start the program by executing the following code cells
    a.
        !nvidia-smi
    b.
        !nvcc -arch=sm_75 -gencode=arch=compute_75,code=sm_75 lab8.cu -o lab8
        !./lab8